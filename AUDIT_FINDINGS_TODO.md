# 📋 تقرير الفحص الشامل وقائمة المهام المستقبلية (Production Audit & Action Items)
**تاريخ الفحص:** سبتمبر 2026  
**بيئة العمل والإنتاج:** `platform.autommerce.com`  
**الحالة:** تم فحص وتدقيق كافة صفحات المنصة البرمجية سطراً بسطر في الـ Frontend والـ Backend وقاعدة البيانات Supabase.

---

## 📌 فهرس الصفحات المفحوصة
1. [Products & Upload](#1-products--upload)
2. [Categories](#2-categories)
3. [Catalog Intelligence (Enrichment)](#3-catalog-intelligence)
4. [Image Classification](#4-image-classification)
5. [Products Gallery](#5-products-gallery)
6. [Products Visualizer](#6-products-visualizer)
7. [Market Research](#7-market-research)
8. [Customize (Storefront Widgets)](#8-customize)
9. [Growth Sync](#9-growth-sync)
10. [Wallet](#10-wallet)
11. [Website Restructure](#11-website-restructure)
12. [Store Assistant](#12-store-assistant)
13. [Usage & AI Credits](#13-usage--ai-credits)
14. [Team Management](#14-team-management)
15. [Settings & Integrations](#15-settings--integrations)
16. [Subscription & Billing](#16-subscription--billing)
17. [Dashboard Overview & Workspaces Hub](#17-dashboard-overview--workspaces-hub)

---

## 1. Products & Upload
* **المسار:** `/w/[slug]/products` و `/w/[slug]/products/upload`
* **طبيعة العمل:** معالجة فورية عبر الـ API وقاعدة البيانات `workspace_products`.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **تحديث المنتجات (Update Existing):** الخلايا الفارغة في الملف لم تعد تمسح بيانات الكتالوج؛ القيمة القديمة تبقى ما لم يفعّل التاجر خيار المسح الصريح.
* **تحديد الكل (Select All):** يعمل على الصفحة الحالية المعروضة فقط (Pagination) وليس على كامل الكتالوج.
* **رفع الكتالوج (Upload Payload):** يتم إرسال ملف المنتجات بالكامل كـ JSON في طلب واحد (`proxyClientMaxBodySize: 10mb`).
* **معالج الرفع القديم (Upload Wizard):** معالج رفع المنتجات يعتمد على الاكتشاف التلقائي للـ SKU، ولم يتم ترقيته بعد بنظام مطابقة الأعمدة الاحترافي (Column Mapper) الذي تم بناؤه للتصنيفات.
* **معالجة الأخطاء:** تظهر بعض أخطاء 409 و 402 كـ Browser Alerts بدلاً من رسائل Toasts تفاعلية.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] تعميم معالج مطابقة الأعمدة المتقدم (Column Mapping Wizard) على صفحة رفع المنتجات.
- [x] إضافة حماية لتحديث المنتجات بحيث لا يتم مسح الحقول الحالية إذا كانت فارغة في الملف المرفوع إلا إذا طلب التاجر ذلك صراحة.
- [ ] تحسين زر "Select All" ليتيح تحديد كامل الكتالوج عبر السيرفر.

---

## 2. Categories
* **المسار:** `/w/[slug]/categories`
* **طبيعة العمل:** شجرة تصنيفات تفاعلية مرتبطة بـ `workspace_categories` وتدعم السحب والإفلات.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **حفظ التغييرات:** السحب والإفلات وتعديل الشجرة يتطلب ضغط زر **Save Changes** يدوياً لحفظ الترتيب في قاعدة البيانات.
* **القفل التفاؤلي (Optimistic Concurrency):** إذا قام مستخدمان بتعديل التصنيفات في نفس الوقت، يتم رفض التعديل الأخير بـ 409 لمنع تضارب البيانات.
* **تم إصلاحه مؤخراً:** تم بناء نظام Column Mapping دقيق لملفات CSV الخاصة بشوبيفاي وووكومرس مع قراءة الـ Title والـ Handle بدقة.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] إضافة تنبيه بصري واضح عند وجود تعديلات غير محفوظة قبل مغادرة الصفحة.

---

## 3. Catalog Intelligence
* **المسار:** `/w/[slug]/catalog-intelligence`
* **طبيعة العمل:** Background Worker يعتمد على جدول `job_runs` و 8 عمال متوازيين.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **مغادرة الصفحة:** آمنة تماماً؛ الجوبة تعمل على السيرفر ويتم عمل Checkpoint كل 50 صفاً أو 30 ثانية.
* **نفاد الرصيد:** تتحول الجوبة تلقائياً إلى حالة `paused_no_credits`.
* **تطبيق النتائج على الكتالوج:** يعتمد على Compare-And-Swap لمنع الكتابة المزدوجة.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] إضافة لافتة توضيحية وزر سريع لاستئناف الجلسات المتوقفة بسبب نفاد الرصيد بمجرد تجديد الاشتراك أو شحن الكريديتس.

---

## 4. Image Classification
* **المسار:** `/w/[slug]/image-classify`
* **طبيعة العمل:** معالجة خلفية عبر `after()` باستخدام نموذج Gemini 3.6 Flash.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **الحد الأقصى:** محدد بـ **200 صورة كحد أقصى** في الجلسة الواحدة.
* **أسماء الملفات:** يتم تنظيف أسماء الصور؛ الأسماء الطويلة جداً الشبيهة بالعناوين تُرفض تلقائياً.
* **ضغط الصور:** المتصفح يضغط الصور المصغرة إلى أقل من أو يساوي 1024px JPEG لتوفير النطاق الترددي.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] دعم رفع ملفات الأرشيف (ZIP Files) لرفع مئات الصور دفعة واحدة.
- [ ] رفع سقف الـ 200 صورة للعملاء أصحاب الكتالوجات الضخمة.

---

## 5. Products Gallery
* **المسار:** `/w/[slug]/products-gallery`
* **طبيعة العمل:** Background Job متصل بـ `job_runs` مع حجز مسبق للرصيد وتوليد صور الذكاء الاصطناعي.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **مغادرة الصفحة:** آمنة تماماً.
* **مزامنة الـ Polling:** يعتمد على نظام `snapshot-clock` الأحادي الاتجاه لمنع تضارب النسخ عند عودة المتصفح.
* **الحد الأقصى:** 5,000 صف في ملف الرفع (CSV/XLSX حتى 20 ميغابايت) — `UPLOAD_LIMITS.gallery` في `src/lib/upload-limits.ts`. بالإضافة إلى ذلك، هناك سقف ثانٍ متغيّر حسب باقة التاجر (`plan.max_products_per_workspace` عبر `assertJobRowQuota`)؛ فحتى لو كان الملف أقل من 5,000 صف، قد يُرفض الرفع إذا تجاوز سقف الباقة.

---

## 6. Products Visualizer
* **المسار:** `/w/[slug]/products-visualizer`
* **طبيعة العمل:** توليد نصوص وأوصاف تسويقية عبر OpenAI ثم تركيب صور المنتجات عبر Gemini.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **مغادرة الصفحة:** آمنة تماماً (Background Job).
* **التوقف المبكر:** في حال إلغاء الجوبة أو توقف توليد الصور، يتم الحفاظ على الأوصاف النصية المولدة في الـ Sheet بدون فقدانها.
* **الحد الأقصى:** 5,000 صف في ملف الرفع (CSV/XLSX حتى 20 ميغابايت) — نفس سقف Gallery تماماً (`UPLOAD_LIMITS.visualizer` في `src/lib/upload-limits.ts`، وليس 1,000 كما ذُكر سابقاً بالخطأ). كذلك يخضع لسقف باقة التاجر الإضافي (`max_products_per_workspace`) عبر `assertJobRowQuota`.

---

## 7. Market Research
* **المسار:** `/w/[slug]/market-research`
* **طبيعة العمل:** 7 مراحل متتالية (Niche -> Scope -> Seeds -> Extract -> Cluster -> On-page -> Push).

### 🔍 الملاحظات والمخاطر التشغيلية:
* **مغادرة المتصفح في المرحلة الرابعة (Extraction):** السحب من Apify يعمل على السيرفر عبر `job_runs.kind = mr_extract`. المؤشر محفوظ في `mr_runs.next_cursor`، والجوبة تُقسَّم إلى مضخات 45 ثانية مع heartbeat. إغلاق التبويب لم يعد يوقف التسوية؛ الحجز (`held`) يُسوّى عند اكتمال كل البذور أو الإلغاء أو بعد 24 ساعة. الواجهة تسأل الحالة كل 800ms للعرض فقط، ويمكنها تسريع السحب إن لم يكن العامل ماسكاً الـ lease. إن لم تُطبَّق هجرة SQL بعد، يفشل إنشاء `job_run` بهدوء ويعود الـ poll القديم للمتصفح (مع مؤشر العميل كاحتياط).
* **حفظ المراحل (Slices):** يتم الحفظ التلقائي (~900ms) في Storage؛ فتح المشروع من جهازين في نفس الوقت قد يؤدي إلى مسح تعديلات أحدهما للآخر.
* **عرض الكلمات في الواجهة:** محدد بـ 1,500 كلمة كعينة في المتصفح لمنع ثقل الواجهة، مع إمكانية تصدير كامل الكلمات عبر زر Export CSV. بعد انتهاء الاستخراج تُحفظ العينة على السيرفر حتى لو أُغلق المتصفح.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [x] **(سهل جداً):** إضافة لافتة استئناف/إلغاء في واجهة المرحلة 4 للمشاريع التي لديها حجز رصيد معلق (`held`).
- [ ] **(سهل - متوسط):** إضافة Optimistic Locking عند حفظ الـ Slices لمنع الكتابة المزدوجة من أكثر من مستخدم.
- [x] **(متوسط - هيكلي):** تحويل سحب الكلمات وتسوية المحفظة إلى Worker خلفي مستقل لا يعتمد على بقاء المتصفح مفتوحاً.

---

## 8. Customize (Storefront Widgets)
* **المسار:** `/w/[slug]/customize`
* **طبيعة العمل:** تخصيص مظهر ويدجت الأسئلة الشائعة والروابط الداخلية لصفحات المتجر.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **سباق التحميل (Race Condition):** الصفحة تقرأ أولاً من localStorage ثم يكتب فوقها السيرفر عند استجابة الـ DB؛ التعديل السريع فور الفتح قد يُمسح بوصول رد السيرفر.
* **مطابقة الهاندل الصارمة (Strict Handle Match):** الويدجت على المتجر يبحث حصرياً عن الكولكشنات التي دُفعت من Market Research ولها `storeHandle` مسجل. الكولكشنات المنشأة يدوياً في شوبيفاي لن يعرض الويدجت فيها أي أسئلة وسيعود بـ `faqs: []`.
* **تباين الكاش:** شكل الويدجت (الألوان والخطوط) يتحدث حياً فوراً، بينما نصوص الأسئلة والروابط لها كاش CDN ومتصفح من 30 إلى 60 ثانية.
* **غياب تحذير المغادرة:** لا يوجد `beforeunload` لتنبيه المستخدم عند الخروج بدون حفظ التعديلات.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] إضافة تحذير عند مغادرة الصفحة إذا كانت هناك تعديلات ألوان لم تُحفظ.
- [ ] موازنة الـ State لمنع استبدال تعديلات المستخدم في حال تأخر جلب إعدادات السيرفر.

---

## 9. Growth Sync
* **المسار:** `/w/[slug]/growth-sync`
* **طبيعة العمل:** معالجة سريعة للمنتجات الجديدة عبر HTTP Request بمهلة 60 ثانية أو عبر تكة Cron دورية كل 24 ساعة.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **سقف الـ 100 منتج:** التشغيل محدد بـ 100 منتج لكل دورة (`MAX_PRODUCTS_PER_RUN = 100`). إذا كان هناك 300 منتج جديد في المتجر، يحتاج التاجر للضغط على "Run now" ثلاث مرات في الوضع اليدوي.
* **الاستيراد الضخم (Truncated Walk):** استيراد آلاف المنتجات دفعة واحدة إلى المتجر يؤدي إلى فشل المزامنة بـ `RuleConfigError` وتعليق القاعدة لحماية دقة العلامة المائية.
* **نفاد رصيد المحفظة يعطل القاعدة:** إذا لم يتوفر رصيد، تفشل الجولة وتتحول القاعدة إلى **معطلة (`disabled: true`)**، ولن تعمل مجدداً بعد شحن المحفظة إلا بعد تفعيل المفتاح يدوياً.
* **غياب وضع المراجعة (No Review Mode):** يتم ربط وتصنيف المنتجات في المتجر الحي مباشرة بدون شاشة مراجعة مسبقة (خيار التراجع متاح عبر زر Undo في سجل النشاط).

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] إضافة إشعار وتنبيه بالبريد أو في المنصة عندما يتم تعطيل قاعدة مزامنة بسبب نفاد الرصيد.
- [ ] تفعيل وضع المراجعة (Review Mode) قبل النشر للمتاجر التي تفضل الموافقة اليدوية.

---

## 10. Wallet
* **المسار:** `/w/[slug]/wallet`
* **طبيعة العمل:** محفظة نقدية بالدولار الأمريكي (USD Pay-As-You-Go) محمية بقيد قاعدة بيانات `balance_usd >= 0`.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **الشحن التلقائي (Auto-Reload):** الإعداد مخزن في قاعدة البيانات لكنه غير مربوط بعمليات خصم تلقائية عبر بطاقة Stripe حتى الآن؛ الشحن يجب أن يتم يدوياً.
* **سقف تصدير الكشف (CSV Export):** يقتصر التصدير على أحدث 5,000 معاملة (50 صفحة).
* **أمان المعاملات:** العمليات المالية آمنة تماماً ومبنية بنظام `idempotencyKey` لمنع تكرار الخصم أو السحب المزدوج.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] ربط ميزة الشحن التلقائي (Auto-Reload) مع Stripe Off-session Payments.
- [ ] دعم تصدير كشوف الحساب الضخمة مع فلترة النطاق الزمني (Date Range Picker).

---

## 11. Website Restructure
* **المسار:** `/w/[slug]/website-restructure`
* **طبيعة العمل:** بناء ترويسة المتجر (Header & Mega Menu) عبر بث متسلسل (Vision -> Research -> Code Generation) باستخدام Gemini 3.7 Flash.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **حد المشاريع التراكمي (Lifetime Limit):** عدد المشاريع محسوب لمدى الحياة بناءً على الباقة؛ حذف مشروع قديم لا يعيد لك خانة شاغرة.
* **سقف رسائل التعديل:** مسموح بـ 10 رسائل تعديل للشات لكل مشروع (`WR_MAX_EDIT_MESSAGES = 10`).
* **انقطاع الاتصال أثناء البناء:** القفل محدد بـ 3 دقائق (`build_lease_until`)؛ إذا أُغلق المتصفح أثناء التوليد، تظل الجلسة مقفلة لـ 3 دقائق قبل أن تتاح إعادة المحاولة.
* **التنزيل المستقل:** كود الهيدر النهائي يتم تصديره كملف HTML مستقل يدمج الشعار كـ Base64 ليعمل مدى الحياة دون روابط خارجية منتهية الصلاحية.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] إضافة مؤشر واضح لعدد المشاريع المتبقية ورسائل التعديل المستخدمة في الواجهة.

---

## 12. Store Assistant
* **المسار:** `/w/[slug]/store-assistant`
* **طبيعة العمل:** وكيل ReAct تفاعلي متعدد الأدوات مع جدول بيانات تفاعلي (Sync Sheet) واتصال مباشر بالمتجر.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **مهلة الطلب:** الحد الأقصى للجولة الواحدة في الشات هو 5 دقائق (`maxDuration = 300`).
* **نظام نقاط الحفظ (Checkpoint System):** يتم حفظ التقدم في Storage **كل 20 صفاً فقط** (`shouldWriteCheckpoint` في `src/lib/sync/agent/checkpoint.ts`) — لا يوجد أي مؤقّت زمني موازٍ (بخلاف Catalog Intelligence التي تعتمد على صفوف *و* وقت معاً). هذا يعني أنه إذا كان معالجة الصف الواحد بطيئة (مثلاً بحث صور معقد)، فقد تمر دقائق دون أي Checkpoint جديد قبل اكتمال 20 صفاً، وعند انقطاع الاتصال في تلك الأثناء يُفقد التقدم منذ آخر نقطة حفظ.
* **حماية الروابط:** الأعمدة الحساسة مثل `handle` و `id` محمية برمجياً لمنع مسحها أو تعديلها بالخطأ.
* **محركات الصور:**
  - الوضع السريع: يعتمد على محرك البحث السريع.
  - الوضع الاحترافي (Pro Mode): يستخدم **OpenAI Sol (`gpt-5.6-sol`)** مع البحث الداخلي لضمان دقة اختيار الصور.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] إضافة زر صريح وواضح في الواجهة لتصدير الـ Sheet إلى Excel / CSV في أي لحظة أثناء المحادثة.
- [ ] إضافة مؤقّت زمني (مثلاً كل 30 ثانية) موازٍ لعدّاد الصفوف الحالي في `shouldWriteCheckpoint`، بحيث لا يعتمد الحفظ على عدد الصفوف فقط؛ هذا يمنع فقدان تقدم كبير عند معالجة صفوف بطيئة قبل انقطاع الاتصال.

---

## 13. Usage & AI Credits
* **المسار:** `/w/[slug]/usage`
* **طبيعة العمل:** سجل استهلاك رصيد الذكاء الاصطناعي (Subscription Credits) وتقارير تفصيلية حسب العضو والعملية والتاريخ.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **الاستهلاك الكلي التاريخي:** يتم حسابه بدقة عبر Postgres RPC `credit_usage_totals` لمنع أخطاء الحساب المحلي.
* **التصدير والفلترة:** يدعم تصدير CSV حتى 5,000 سجل، مع فلترة دقيقة حسب التاريخ والعضو.
* **الخصم المالي:** العمليات تتم عبر RPC ذري `deduct_user_credits` يمنع تجاوز الرصيد المتاح.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] الصفحة سليمة ومحمية بالكامل (`✅ Clean & Solid`).

---

## 14. Team Management
* **المسار:** `/w/[slug]/team` و `/invite/[token]`
* **طبيعة العمل:** إدارة أعضاء مساحة العمل والدعوات والأدوار الصلاحية (`owner`, `admin`, `editor`, `viewer`).

### 🔍 الملاحظات والمخاطر التشغيلية:
* **أمان ترقية الأدوار:** محمي برمجياً على مستوى السيرفر؛ المالك (Owner) فقط يستطيع ترقية الأعضاء إلى رتبة Admin.
* **سجل الأمان والتدقيق:** كل تغيير في الأدوار أو حذف عضو يتم تسجيله في جدول `security_audit_logs`.
* **حدود المقاعد (Seat Limits):** يتم التحقق من سقف الباقة `max_members_per_workspace` قبل إرسال أي دعوة جديدة.
* **صلاحية الدعوة:** رمز الدعوة مشفر بـ `crypto.randomBytes(32)` وينتهي تلقائياً بعد 7 أيام، ويشترط تطابق البريد الإلكتروني مع الحساب المقبول.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] الصفحة سليمة ومحمية بالكامل (`✅ Clean & Solid`).

---

## 15. Settings & Integrations
* **المسار:** `/w/[slug]/settings`
* **طبيعة العمل:** ضبط إعدادات مساحة العمل، ربط المتاجر (Shopify / WooCommerce)، وحذف مساحة العمل.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **تشفير مفاتيح الربط (Encrypted Credentials):** الـ API Tokens مشفرة بخوارزمية **AES-256-GCM** مع مفاتيح دوارة، وتُعرض للمستخدم مقنّعة كـ `••••••••` دون كشف الـ Token الأصلي أبداً.
* **عزل النطاقات (Multi-Tenant Domain Isolation):** دالة `replaceWorkspaceDomains` تضمن عدم تكرار ربط نفس الدومين في مساحتي عمل مختلفتين.
* **حذف مساحة العمل (Safe Purge):** مسار الحذف مقتصر على المالك فقط، ويقوم بحذف كافة الملفات في Supabase Storage أولاً والتأكد من خلوها قبل حذف الصفوف في قاعدة البيانات لتفادي بقاء ملفات يتيمة (Orphaned Blobs).

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] الصفحة سليمة ومحمية بالكامل (`✅ Clean & Solid`).

---

## 16. Subscription & Billing
* **المسار:** `/w/[slug]/subscription`
* **طبيعة العمل:** ترقية وتغيير باقات الاشتراك وشراء باقات إضافية من الـ Credits عبر Stripe Billing Portal و Stripe Checkout.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **بوابة الدفع:** تستخدم Stripe Hosted Checkout و Customer Portal.
* **شراء رصيد إضافي (Bonus Credits):** محدد بحد أدنى 100 كريديت (`CREDIT_TOPUP_MIN_CREDITS = 100`) ويشترط وجود اشتراك نشط.
* **مزامنة الـ Webhooks:** تفعيل الاشتراك وإضافة الرصيد يتم عبر Webhooks موثقة ومحمية بعدم التكرار (`webhook_events`).

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] الصفحة سليمة ومحمية بالكامل (`✅ Clean & Solid`).

---

## 17. Dashboard Overview & Workspaces Hub
* **المسار:** `/w/[slug]` (الرئيسية) و `/workspaces` و `/workspaces/new`
* **طبيعة العمل:** لوحة القيادة المركزية، ملخص الأداء والرصيد والمحفظة، والتنقل بين مساحات العمل.

### 🔍 الملاحظات والمخاطر التشغيلية:
* **التحقق من سقف مساحات العمل:** مسار `/api/workspaces/limit` يمنع إنشاء مساحات عمل إضافية تتجاوز حد الباقة المسموح (`max_workspaces`).
* **استقبال الدعوات:** صفحة مساحات العمل تعرض الدعوات المعلقة للمستخدم مع إمكانية قبولها بضغطة زر واحدة.

### 🛠️ المهام المقترحة للتطوير (TODO):
- [ ] الصفحة سليمة ومحمية بالكامل (`✅ Clean & Solid`).

---

*تم استكمال تدقيق وفحص كافة صفحات ووحدات المنصة بنجاح وتوثيق جميع الملاحظات البرمجية والتشغيلية.*
